# 路径硬编码清单 + 换机指南

换电脑跑 `python main.py` 时：**只改一份配置** —— 根目录的 `local_config.toml`  
（从 `local_config.example.toml` 复制）。不要改 Python/C 源码里的盘符。

---

## 一、怎么用（最短）

1. 复制 `local_config.example.toml` → `local_config.toml`
2. 改这两处（按你电脑）：

```toml
[paths]
snrobotlab_dir = "C:\\software\\SNRobotLab"   # 搜 snrobot.exe 找到的目录

[duco]
robot_ip = "192.168.1.10"                    # DUCO 真机 IP，没有可先不管
```

3. 把下面两个文件拷进 `snrobotlab_dir`（与 `snrobot.exe`、`RobotSDK.dll` 同目录）：
   - `tools/scara_do/scara_do.exe`
   - `tools/scara_enable/scara_enable.exe`
4. `python main.py`

`local_config.toml` 已加入 `.gitignore`，每人本地一份，互不覆盖。

---

## 二、两个 C 编译 exe 内部路径检查（2026-08-11）

| 文件 | 是否写死盘符路径 | 说明 |
|------|------------------|------|
| `tools/scara_do/scara_do.exe` | **否**（已用当前源码重编） | 运行时用「自身所在目录」找 `RobotSDK.dll`；错误信息里可能出现文字 `SNRobotLab`，不是盘符 |
| `tools/scara_enable/scara_enable.exe` | **否** | 同上 |
| 二进制里的 `D:\W\B\src\build-UCRT64` / `GCC: ... MSYS2` | 可忽略 | 编译器自带的调试字符串，**运行时不会用来找文件** |

历史问题：旧版 `scara_do.exe` 曾写死 `D:\SNRobotLab`，会报 `SetCurrentDirectory(D:\SNRobotLab) 失败`。  
请务必使用仓库里**重新编译后**的 `tools/scara_do/scara_do.exe`。

源码（无硬编码盘符，仅注释/提示里出现目录名）：

- `tools/scara_do/scara_do.c`
- `tools/scara_enable/scara_enable.c`

---

## 三、硬编码标记总表

凡影响「换机跑 main.py」的项，应以 `local_config.toml` 为准。  
其余标了 `HARDCODED_PATH` 的是自测/仅开发编译用，**正常运行不要改**。

### A. 运行时路径 —— 已统一走 local_config.toml

| 位置 | 原先问题 | 现状 |
|------|----------|------|
| `src/scara/config/scara_config.py` | 默认 `D:\SNRobotLab` | **已取消盘符默认**；读 `[paths].snrobotlab_dir` |
| `src/scara/controller/do_client.py` | 默认/环境变量分叉 | 跟控制器传入的 `sdk_dir` / toml |
| `src/scara/controller/enable_client.py` | 同上 | 同上 |
| `src/scara/controller/scara_controller.py` | 用 config.exe_dir | 已用 toml 加载的配置 |
| `tools/uitest/enable_client.py` | 默认 `D:\SNRobotLab` | **已改为**读同一套 `resolve_snrobotlab_dir()` |
| `src/devices/robot_arm/__init__.py` | DUCO IP 只在 yaml | **已支持**用 toml `[duco]` 覆盖 |

### B. 仍含硬编码，但不影响「只改 toml 跑 main.py」

| 位置 | 标记 | 说明 |
|------|------|------|
| `tools/scara_do/build.bat` | `HARDCODED_PATH(dev-only)` | MSYS2 的 gcc 探测路径；`SDKDIR` 无 toml 时才回退 `D:\SNRobotLab`。**别人跑程序不用编 C** |
| `tools/scara_enable/build.bat` | 同上 | 同上 |
| `src/robot_arm/ui/console_widget.py` 末尾 `__main__` | `HARDCODED_PATH` | `G:/2D_robotics/...` 仅单独自测截图 |
| `src/devices/robot_arm/calib/wafer_tray.py` 自测 | `HARDCODED_PATH` | `C:/Users/Lenovo/...` 仅该文件自测 |
| `src/devices/robot_arm/robot_arm.yaml` | 默认 IP/URL | 可被 `local_config.toml` `[duco]` 覆盖；未建 toml 时用 yaml 默认值 |
| `README.md` / 旧文档举例 | 文档示例盘符 | 以本文件与 `local_config.example.toml` 为准 |

### C. 不是「本机文件夹路径」的地址（可选改 toml）

| 内容 | 在哪改 |
|------|--------|
| SCARA 控制器 IP/端口 | `local_config.toml` → `[scara]` |
| DUCO 真机 IP / armweb | `local_config.toml` → `[duco]` |
| 界面里默认的 `http://127.0.0.1:8080` | 本机回环，一般不用改；也可改 toml |

---

## 四、部署检查清单

SNRobotLab 目录（`snrobotlab_dir`）内应有：

- [ ] `snrobot.exe`
- [ ] `scara_do.exe`（仓库最新版）
- [ ] `scara_enable.exe`
- [ ] `RobotSDK.dll` 及官方依赖
- [ ] 许可证（如 `SiaSunRobot.lic`）
- [ ] （建议）`WorkStationConfig.json` Active 工位正确

程序根目录：

- [ ] 已有按本机改好的 `local_config.toml`
- [ ] `python main.py` 能开界面；SCARA 能连接；DO 清零不再报 `D:\SNRobotLab`

---

## 五、环境变量（可选，一般不用）

| 变量 | 作用 |
|------|------|
| `SNROBOTLAB_DIR` | 临时覆盖 toml 里的 SNRobotLab 目录 |
| `SCARA_DO_EXE` / `SCARA_ENABLE_EXE` | 指定单个 exe 全路径（仍须与 `RobotSDK.dll` 同目录） |

推荐日常只用 `local_config.toml`，不设这些变量。
