# 换电脑跑 main.py：路径要改哪里？

别人电脑上 SNRobotLab（新松官方软件目录）不一定在 `D:\SNRobotLab`。
本说明列出：**哪些路径会影响运行**、**改哪个文件**、**两个小 exe 要不要重新编译**。

---

## 先记住一件事

| 程序 | 干什么 | 要不要在别人电脑上编译 C？ |
|------|--------|---------------------------|
| `snrobot.exe` | 主控制桥（连控制器、运动） | 否（官方/已有程序） |
| `scara_do.exe` | 写泵/阀（DO） | **否**，复制现成 exe 即可 |
| `scara_enable.exe` | 使能 / 去使能 / 急停 / 清报警 | **否**，复制现成 exe 即可 |

`scara_do` / `scara_enable` 的 C 源码**没有写死** `D:\...`。
它们启动后问操作系统：「我这个 exe 自己在哪？」然后只在**同一文件夹**里找 `RobotSDK.dll` 和配置。
所以别人电脑**不需要装 gcc / C 编译器**，只要把已经编好的 exe 拷进对方的 SNRobotLab 目录。

路径由 **Python 配置** 决定「去哪个目录找这些 exe」。

---

## 换机最短步骤（推荐）

1. 在对方电脑装好 / 找到 SNRobotLab（里面应有 `RobotSDK.dll`、许可证 `SiaSunRobot.lic` 等）。
2. 把本仓库里的这两个文件拷进去（与 `RobotSDK.dll` 同目录）：
   - `tools\scara_do\scara_do.exe`
   - `tools\scara_enable\scara_enable.exe`
3. 确认对方目录里也有 `snrobot.exe`（和许可证同目录，不能单独乱拷走）。
4. 复制 `scara_config.example.toml` → 改名为程序根目录的 `scara_config.toml`。
5. 只改里面这一行成对方真实路径，例如：

```toml
exe_dir = "C:\\software\\SNRobotLab"
```

（Windows 路径在 toml 里要用双反斜杠 `\\`。）

6. 在程序根目录运行：`python main.py`

一般**只改 `scara_config.toml` 的 `exe_dir` 就够**。  
`scara_do` / `scara_enable` 会跟 `snrobot` 用同一个目录解析逻辑。

---

## 路径清单（按「要不要改」）

### 1. 必看：SNRobotLab 目录（三个 exe 共用）

**作用**：告诉程序 `snrobot.exe`、`scara_do.exe`、`scara_enable.exe` 在哪。

| 改哪里 | 说明 |
|--------|------|
| **`scara_config.toml`**（根目录，从 example 复制）里的 `connection.exe_dir` | **推荐**。改这一处即可覆盖 snrobot + 两个小工具的默认目录 |
| 环境变量 **`SNROBOTLAB_DIR`** | 优先级最高，临时覆盖 toml；适合不改文件时测试 |
| `src\scara\config\scara_config.py` 里的 `DEFAULT_EXE_DIR` | 仅当没有 toml、也没有环境变量时的代码默认值（现在是 `D:\SNRobotLab`） |

解析顺序（高 → 低）：

1. `SNROBOTLAB_DIR`
2. `scara_config.toml` → `exe_dir`
3. `DEFAULT_EXE_DIR`（代码默认）

相关代码：

- `src\scara\config\scara_config.py` → `resolve_snrobotlab_dir()`、`load_scara_config()`
- `src\scara\controller\do_client.py` → `sdk_dir()` / `exe_path()`
- `src\scara\controller\enable_client.py` → `sdk_dir()` / `exe_path()`

可选：只覆盖某一个小工具的完整路径：

| 环境变量 | 作用 |
|----------|------|
| `SCARA_DO_EXE` | 直接指定 `scara_do.exe` 完整路径 |
| `SCARA_ENABLE_EXE` | 直接指定 `scara_enable.exe` 完整路径 |

注意：这两个小 exe **仍然必须和 `RobotSDK.dll`、许可证放在同一目录**（SDK 按「exe 所在目录」找文件）。  
环境变量改的是「Python 去启动哪个 exe」，不是让 exe 去另一个盘找 DLL。

---

### 2. 控制器 IP / 端口

| 位置 | 说明 |
|------|------|
| SNRobotLab 里的 **`WorkStationConfig.json`** | 官方工位配置；`scara_do` / `scara_enable` 在没传 `--ip` 时会读这里的 Active 工位 |
| `scara_config.toml` 的 `controller_ip` / `controller_port` | 主要给界面显示；Python 调两个小 exe 时会把 IP/端口用命令行传进去 |
| `src\scara\config\scara_config.py` 默认 `192.168.1.100:20002` | 无 toml 时的默认显示值 |

真机 IP 变了：先改官方工位配置，或保证 Python 传入的 IP/端口正确。

---

### 3. 仅「自己重新编译」时才改（别人换机通常不用）

| 文件 | 写死了什么 |
|------|------------|
| `tools\scara_do\build.bat` | 默认部署到 `D:\SNRobotLab`；可用环境变量 `SNROBOTLAB_DIR` 覆盖。另含本机 `D:\MSYS2\...` 的 gcc 探测路径 |
| `tools\scara_enable\build.bat` | 同上 |

别人电脑没有 C 编译器时：**不要跑 build.bat**，直接复制仓库里已有的 `.exe`。

---

### 4. 独立测试脚本（跑 main.py 时一般碰不到）

| 文件 | 说明 |
|------|------|
| `tools\uitest\enable_client.py` | 单独测使能用的副本，默认仍看 `SNROBOTLAB_DIR` / `D:\SNRobotLab` |
| `tools\uitest\README.md` | 文档里举例写过 `D:\SNRobotLab` |
| `src\robot_arm\ui\console_widget.py` 末尾自测 | 写死了 `G:\2D_robotics\...`，**只影响该文件单独自测截图**，不影响 `main.py` |

---

### 5. 文档里曾出现过不一致（以配置为准）

`README.md` 里 SCARA 一节曾写过 `C:\software\SNRobotLab`，代码默认是 `D:\SNRobotLab`。  
**以对方机器真实安装目录 + `scara_config.toml` 为准**，不要死记某一个盘符。

---

## 第二个任务结论：C 源码里有没有内部路径？

源码位置：

- `tools\scara_do\scara_do.c`
- `tools\scara_enable\scara_enable.c`

检查结果：

- **没有**把 `D:\SNRobotLab` 写进可执行逻辑。
- 用 Windows API `GetModuleFileName` 得到「本 exe 所在文件夹」，再加载同目录的 `RobotSDK.dll`，并读取同目录的 `WorkStationConfig.json`（若命令行没给 `--ip/--port`）。
- 注释里以前写过「必须放在 D:\SNRobotLab」，容易误解；已改成「放在与 DLL/许可证同目录」。
- **IP/端口**：可由命令行 `--ip` / `--port` 传入；Python 的 `do_client` / `enable_client` 已经在传，不依赖 C 内部写死 IP。

因此：**路径配置放在 Python（toml / 环境变量）即可，换机不必改 C、不必重编。**

---

## 部署检查清单

对方机器上，SNRobotLab 目录里至少应有：

- [ ] `snrobot.exe`
- [ ] `scara_do.exe`（从本仓库 `tools\scara_do\` 复制）
- [ ] `scara_enable.exe`（从本仓库 `tools\scara_enable\` 复制）
- [ ] `RobotSDK.dll`（及官方其它依赖 DLL）
- [ ] 许可证（如 `SiaSunRobot.lic`）
- [ ] （建议）`WorkStationConfig.json` 里 Active 工位 IP/端口正确

程序根目录：

- [ ] 已按本机改好 `scara_config.toml`（至少 `exe_dir`）
- [ ] `python main.py` 能启动；SCARA 页能连接
