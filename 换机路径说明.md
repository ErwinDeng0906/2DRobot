# 换机说明（简版）

**完整清单与硬编码标记见 [`路径硬编码清单.md`](路径硬编码清单.md)。**

## 只改一个文件

1. 复制 `local_config.example.toml` → `local_config.toml`
2. 修改：

```toml
[paths]
snrobotlab_dir = "你的\\SNRobotLab路径"
```

3. 把 `tools/scara_do/scara_do.exe`、`tools/scara_enable/scara_enable.exe`，'tools\snrobot bridge\snrobot.deps.json.bak.json'注意这里要bak禁用）、'tools\snrobot bridge\snrobot.dll'、'tools\snrobot bridge\snrobot.exe'、'tools\snrobot bridge\snrobot.runtimeconfig.json' 拷进该目录  
4. `python main.py`

两个小 exe **不要**从 `tools\` 直接运行（旁边没有 `RobotSDK.dll`）。  
旧版 `scara_do.exe` 曾写死 `D:\SNRobotLab`，请用仓库里重新编译后的版本。
