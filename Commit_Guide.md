# 🚀 代码提交与协作指南 (给新手的 Git 快速上手教程)

欢迎参与 **RobotArm_SCARA_Control** 项目的开发！为了保证项目代码的稳定与干净，请大家**切勿直接向 `main` 主线分支提交代码**。

请严格按照以下 **4 个步骤** 进行代码的修改与提交。

---

## 🛠️ 第一步：开始写代码前（新建你的专属分支）

每次准备写新功能或修 Bug 前，先在项目根目录下打开终端（Terminal / PowerShell），依次运行：

` ` `bash
# 1. 切换到 main 分支
git checkout main

# 2. 拉取云端最新的代码（确保你的基础是最新的）
git pull

# 3. 创建并切换到你的新分支（请将 my-new-feature 换成你这次改动的名字）
git checkout -b feature/你的功能名称
` ` `

> 💡 **为什么要建分支？** 
> 这样你在本地哪怕写崩了代码，也绝对不会影响到主线 `main` 的正常运行！

---

## 💻 第二步：本地修改与提交（敲代码并盖章保存）

在编辑器里改好代码、并测试无误后：

` ` `bash
# 1. 把所有修改过的文件放入暂存区
git add .

# 2. 填写这次修改了什么（提交说明）
git commit -m "简短说明你改了什么，例如：修复了水泵控制按钮卡死的bug"
` ` `

---

## 🌐 第三步：推送你的分支到 GitHub 云端

把你在本地写好的分支上传到 GitHub：

` ` `bash
git push -u origin feature/你的功能名称
` ` `
*(注意：请把 `feature/你的功能名称` 替换成你在第一步里起的分支名字)*

---

## 🔀 第四步：在 GitHub 网页上发起合并请求 (Pull Request)

1. 打开项目的 GitHub 页面。
2. 页面上方会出现一个黄色的提示框，显示你刚刚推上来的分支，点击旁边的绿色按钮 **`Compare & pull request`**。
3. 在弹出的页面中：
   * 简单写一下你这次修改了什么内容。
   * 点击 **`Create pull request`**。
4. 项目负责人在 GitHub 上审查无误后，会点击 **Merge** 合并进 `main` 分支。

---

## 常见问题 (FAQ)

* **Q: 我修改完了，需要手动删除我建的分支吗？**
  * A: 当你的 Pull Request 在 GitHub 上被合并后，可以在 GitHub 页面点击 **`Delete branch`** 按钮删除云端分支；本地分支可以用 `git branch -d 分支名` 删除。
* **Q: 只是改了个错别字/文档注释，也需要这么麻烦吗？**
  * A: 如果只是改几字注释，可以联系管理员直接在 `main` 上提交，但写任何**功能性代码**请一律走以上分支流程！
