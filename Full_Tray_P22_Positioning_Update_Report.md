# P22 全盘定位功能更新报告

## 完成的功能

本次新增了一条从托盘任意 XY 位置到 P22 的固定高度粗到细定位链：

`Stage2/P00 托盘几何粗目标 → 一个 IK 粗定位阶段（内部安全短航点） → 5 帧 Stage3 毫米误差 → 一次几何修正 → Task9 P22 局部 Jacobian 有限精修`

当前只授权目标 P22。目标数据、UI 下拉框和报告格式已经按 P00–P55 共 36 槽组织，
后续为其他槽补齐局部 Jacobian 后可以沿用同一架构扩展。

## 新增文件

### `src/scara/vision/full_tray_positioning.py`

纯数学和安全规划模块，不导入控制器、Qt、DO 或真空接口。包含：

- Stage2 槽中心从托盘系转换到机械 world XY；
- Stage3 \({}^C T_T\) 和 Stage4 \({}^C p_S\) 计算吸盘托盘系毫米位置；
- 托盘系毫米误差旋转为 world XY 几何修正；
- 一个绝对粗目标的短航点 IK 路线生成；
- 五帧毫米误差聚合、稳定性/状态绑定/终点域安全门；
- 调用现有固定 J3/Rz 规划器生成一次几何修正候选。

### `src/scara/vision/full_tray_positioning_session.py`

全盘定位会话和 JSON 证据协调器，不持有控制器。包含：

- 锁定 Stage2、Task9、Task11 输入哈希；
- 从点击时的新鲜机器人快照生成粗定位动作；
- 接收运行时 5 帧 Stage3 数据并生成一次几何修正提案；
- 将最后阶段委托给已有 Stage7B/Task9 代码并强制只用局部模型；
- 保存 `full_tray_positioning.json`、图片和 `points.json` 摘要；
- 保存全部 36 槽 world XY，但目前授权列表只含 P22。

### `docs/full_tray_p22_positioning.md`

详细说明坐标系、核心方程、粗定位为何需要内部航点、几何修正安全门、Task9
切换条件、UI 操作、输出文件和当前能力边界。

### `tests/test_full_tray_positioning.py`

新增离线回归，覆盖：

- Stage3/Stage4 毫米坐标方程；
- P00/P05/P50/P55 到 P22 的粗路线；
- 端点步长、真实 J1→J2→J3→J4 中间扫掠、J3/Rz；
- 一次几何修正进入 Task9 锚点；
- 动作序列只含 XY/等待/运行时审核，不含 Z、DO、真空或录像；
- 36 槽数据结构与 P22 单目标授权；
- 会话提案直接经过真实 `ActionWorker` 离线复核并只执行一次；
- 非 P22 目标失败关闭。

## 修改文件

### `src/scara/vision/handeye_interaction.py`

在原 Stage3 合格帧结果中增加：

- \({}^C T_T\)；
- 当前吸盘托盘系毫米点；
- 目标槽托盘系毫米点；
- 三维托盘系毫米误差。

原像素误差、重投影叠加和 Task9 只读修正保持不变。

### `src/scara/vision/stage7b_session.py`

增加可选的 `force_local_only` 模式。普通“单点有限闭环”行为不变；全盘定位使用该
模式，保证最后阶段只能调用 Task9 局部模型，未进入局部域时直接停止。

### `src/scara/ui/action_worker.py`

为“一次 Stage3 毫米几何修正”增加独立、不可由任务放宽的执行器上限；继续由
`ActionWorker` 在下发前重新读取控制器并复核哈希、状态、ΔXY、IK、J3/Rz、报警和
急停。没有把控制器对象交给视觉会话。

### `src/scara/ui/handeye_demo_dialog.py`

- 删除 `Jacobian验证` 按钮；
- 在 `单点有限闭环` 右侧新增 `全盘定位`；
- 全盘定位严格读取左侧目标，当前非 P22 立即拒绝；
- 确认框明确提示“确认左侧选取的是 P22”；
- 会话状态显示粗定位距离/航点数、毫米误差、ΔX/ΔY、预计终点和全部安全门；
- 两个真实运动模式互斥，关闭/超时/异常均失败关闭。

### `src/scara/ui/control_widget.py`

接入全盘定位的启动、停止、运行时请求路由和收尾；仍由通用 `ActionWorker` 独占
机械臂。相机 1/Stage3 在运动会话期间保持实时工作。

### `tests/test_handeye_demo_read_only.py`

更新 UI 断言：验证旧按钮已删除、新按钮存在、36 槽完整，并验证 Stage3 合格帧含
托盘系毫米结果。

## 安全与实现选择

粗定位只计算一个 P22 最终 IK 目标，但没有直接把盘角到 P22 的大关节跳变交给逐轴
控制器。离线复算显示，直接下发时某些起点的中间 XY 扫掠可超过 100 mm；内部
2 mm 航点把所测四角和 11×11 全盘网格的最坏逐轴相邻扫掠限制在约 3.827 mm，
最大瞬时 Rz 偏差约 0.821°。

所有新功能都是固定高度 XY：不包含 `move_xyzr`、Z 下降、DO、真空、录像或吸放片。
一次几何修正最多 3 mm；预计终点必须进入 Task9 P22 每轴 ±1.8 mm 后才允许局部
精修。

## 离线验证结果

- 共享开发工作区全项目单元测试：128 项全部通过；
- 同步后的 Desktop 0812 项目现有测试集合：123 项全部通过（该副本未包含开发
  工作区中的 5 项 Task12 测试文件）；
- 新增全盘定位专项：8 项全部通过，其中一项覆盖完整
  `ActionWorker.run()` 粗航点→毫米修正→局部收敛生命周期；
- Python 编译检查通过；
- 未连接机械臂、相机、DO 或真空硬件；
- 未执行任何真实运动。

实机首次验收仍建议：无硅片、相机 1、固定观察 J3/Rz、T1 低速、从 P22 附近开始，
再逐步扩大到相邻槽和四角，并始终清空整个托盘上方及机械臂逐轴扫掠区域。
